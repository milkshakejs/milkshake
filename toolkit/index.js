var toolkit = {};

toolkits.ArgumentParser = {
  Parse: function(args) {
    if(typeof args == "string") {
      args = args.split(" ");
    }
    for(i=0;i<args.length;i++) {
      
    }
  }
}

module.exports = toolkit;
